--!strict
-- Phase 12 builds the HUD here, from Lua rather than Studio-authored GUI instances:
-- UI iterates most, so it is what most needs to be diffable.

local HudController = {}

function HudController.Init() end

function HudController.Start() end

return HudController
